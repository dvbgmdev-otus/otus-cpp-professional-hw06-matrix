#ifndef MATRIX_TYPES_H
#define MATRIX_TYPES_H

#include <cstddef>
#include <utility>

using MatrixIndex = std::size_t;
using MatrixPosition = std::pair<MatrixIndex, MatrixIndex>;

#endif  // MATRIX_TYPES_H
